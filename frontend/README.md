# Frontend (Vite + React) — Setup

Prereqs: Node 18+, npm

From `frontend/`:

```bash
npm install
npm run dev
```

The app runs on `http://localhost:5173` and posts to `http://localhost:8000/api/trips/plan` by default.
